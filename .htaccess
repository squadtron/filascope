RewriteEngine On

# Redirect www to non-www
RewriteCond %{HTTP_HOST} ^www\.filascope\.app [NC]
RewriteRule ^ https://filascope.app%{REQUEST_URI} [L,R=301]

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
